const mongoose = require('mongoose');
const Airport = require('../models/Airport');
require('dotenv').config();

// No seed data - Admin will add airports through React app
console.log('Airport seed script removed - use React admin panel to add airports');